const Logins = ({ children }) => {
  return (
    <main className="flex items-center justify-center h-screen bg-gradient-to-br from-blue-400 to-green-400">
      {children}
    </main>
  );
};

export default Logins;
